export default function BenchmarkTable({ rows }) { const best=(key,mode)=>{const values=rows.map(row=>Number(row[key]));return mode==='min'?Math.min(...values):Math.max(...values);}; return <div className="table-wrap"><table><thead><tr><th>Algorithm</th><th>Split</th><th>RMSE ↓</th><th>Concordance Index ↑</th><th>Pearson r ↑</th></tr></thead><tbody>{rows.map(row=><tr key={`${row.Split}-${row.Algorithm}`}><td>{row.Algorithm}</td><td><span className="split-tag">{row.Split.replace(' Split (Baseline)','').toLowerCase()}</span></td><td className={Number(row.RMSE)===best('RMSE','min')?'best':''}>{Number(row.RMSE).toFixed(3)}</td><td className={Number(row.CI)===best('CI','max')?'best':''}>{Number(row.CI).toFixed(3)}</td><td className={Number(row['Pearson r'])===best('Pearson r','max')?'best':''}>{Number(row['Pearson r']).toFixed(3)}</td></tr>)}</tbody></table></div>; }
