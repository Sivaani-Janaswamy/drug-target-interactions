import { pipeline, whyCards } from '../content/copy';
import HeroVisual from '../components/HeroVisual';
import { ActionButton, Eyebrow } from '../components/PageIntro';
function Icon({ type }) { if(type==='clock') return <svg className="icon" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" stroke="#1F6F63" strokeWidth="2"/><path d="M12 7v5l3 3" stroke="#1F6F63" strokeWidth="2" strokeLinecap="round"/></svg>; if(type==='shield') return <svg className="icon" viewBox="0 0 24 24" fill="none"><path d="M12 3l8 4v5c0 5-3.5 8-8 9-4.5-1-8-4-8-9V7l8-4z" stroke="#DE9A34" strokeWidth="2"/></svg>; return <svg className="icon" viewBox="0 0 24 24" fill="none"><path d="M4 12h4l2-7 4 14 2-7h4" stroke="#B84B3A" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/></svg>; }
export default function HomePage({ navigate }) { return <div className="page active"><div className="hero"><div><Eyebrow>VTU Major Project · AI &amp; Data Science</Eyebrow><h1>Predicting whether a drug <em>fits</em> its target — before it's ever tested in a lab.</h1><p className="lede">Feed it a drug and a protein. It predicts how tightly they'd bind — tested only on pairs it's never seen before.</p><div className="hero-actions"><ActionButton onClick={()=>navigate('Try the predictor')}>Try a prediction →</ActionButton><ActionButton ghost onClick={()=>navigate('How it works')}>What does this actually mean?</ActionButton></div></div><HeroVisual/></div><section><Eyebrow>The pipeline, in four steps</Eyebrow><div className="pipeline">{pipeline.map(([num,title,desc])=><div className="pstep" key={num}><div className="num">{num}</div><h3>{title}</h3><p>{desc}</p></div>)}</div></section><section><Eyebrow>Why this matters</Eyebrow><div className="grid-3">{whyCards.map(([icon,title,copy])=><div className="card" key={title}><h3><Icon type={icon}/>{title}</h3><p>{copy}</p></div>)}</div></section></div>; }
