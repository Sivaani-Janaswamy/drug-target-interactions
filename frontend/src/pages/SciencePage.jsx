import { Eyebrow } from '../components/PageIntro';
import BenchmarkTable from '../components/BenchmarkTable';
export default function SciencePage({ benchmarks }) { return <div className="page active"><section className="page-intro"><Eyebrow>For evaluators &amp; the curious</Eyebrow><h2>How the models actually compare</h2><p className="lede">Full results across all four algorithms, reported honestly across three evaluation splits.</p></section><BenchmarkTable rows={benchmarks}/><section><Eyebrow>The honest headline</Eyebrow><div className="card headline-card"><h3>Every model gets worse on cold splits — and that's the finding, not a flaw</h3><p>All four algorithms score noticeably lower once drug or protein overlap is removed between train and test. That gap is the real story: it shows how much of a “good-looking” result on a random split was actually just memorized similarity, not genuine generalization.</p></div></section></div>; }
