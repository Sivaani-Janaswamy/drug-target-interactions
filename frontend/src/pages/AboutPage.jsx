import { team } from '../content/copy';
import { Eyebrow } from '../components/PageIntro';
export default function AboutPage() { return <div className="page active"><section className="page-intro"><Eyebrow>VTU Major Project</Eyebrow><h2>About this project</h2><p className="lede">DTI-ML predicts kinase drug-target binding affinity using classical machine learning, evaluated under a cold-split protocol and explained with SHAP feature attribution, built on the public KIBA benchmark.</p></section><div className="team-grid">{team.map(([letter,title,desc])=><div className="member" key={title}><div className="avatar">{letter}</div><h3>{title}</h3><p>{desc}</p></div>)}</div><section><Eyebrow>References this project builds on</Eyebrow><p className="lede">Positioned against DeepDTA, GraphDTA, KronRLS, and SimBoost — comparing classical, feature-engineered ML against deep-learning DTI approaches on the same benchmark.</p></section></div>; }
